{
	"name": "delete_map",
	"type": 1,
	"default_member_permissions": 8,
	"description": "Deletes a map from the available maps.",
    "options": [
        {
			"name": "name",
			"description": "The name of map",
			"type": 3,
			"required": true
		}
    ]
}