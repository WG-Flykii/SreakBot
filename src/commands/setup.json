{
	"name": "setup",
	"type": 1,
	"default_member_permissions": "8",
	"description": "Set up StreakBot",
	"options": [
		{
			"name": "create_quiz_channel",
			"description": "The channel where the 'Create Private Thread' button will be posted",
			"type": 7,
			"required": true
		},
		{
			"name": "quiz_channel",
			"description": "The main channel where quizzes are played and where threads will be created",
			"type": 7,
			"required": true
		},
		{
			"name": "admin_channel",
			"description": "The channel where admins can control the bot",
			"type": 7,
			"required": true
		}
	]
}